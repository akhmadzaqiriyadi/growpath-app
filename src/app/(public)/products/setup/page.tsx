import { ProductSetup } from './ProductSetup';

export const metadata = {
  title: 'Setup Produk | Growpath Tenant',
  description: 'Setup produk awal untuk mulai berjualan',
};

export default function ProductSetupPage() {
  return <ProductSetup />;
}
